const DEFAULT_FIREFLY_SETTINGS = {
    averageOffSeconds: 9,
    offTimeVariation: 5,

    averageOnSeconds: 2,
    onTimeVariation: 0.6,

    averageSizeInPixels: 2.3,
    sizeVariation: 2,

    averageBlurSizeInPixels: 1,
    blurSizeVariation: 1,
}
